const incr = function (str) {
  const arr = [];
  for (let i = 0; i < str.length; i++) {
    arr.push(String.fromCharCode(str.charCodeAt(i) + 1));
  }
  return arr.join('');
};

console.log(incr('hello'));
